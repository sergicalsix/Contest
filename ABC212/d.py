import numpy as np

q = int(input())
ans = np.array([1000000])

for i in range(q):
    A = list(map(int, input().split()))
    if A[0] == 1:
        if
    elif A[0] == 2:
        ans += A[1]
    else:

        ans.remove(del_num)
        print(del_num)
